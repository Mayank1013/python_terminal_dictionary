# python_terminal_dictionary

This is simple terminal app which runs on data.json (dictionary data).
It asks user input word and give the meaning of it.

Features : 
-> Autocorrect suggestions.
-> Readable format output
